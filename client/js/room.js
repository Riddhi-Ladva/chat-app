// ──────────────────────────────────────────────────────────
// client/js/room.js — Join / Leave Room Logic
// Owner: SDP_Riddhi Ladva
// Phase: 4 (feature/client1-connection)
//
// TODO:
//   1. Import { EVENTS } from './events.js'
//   2. Import { socket } from './socket.js'
//   3. On page load: prompt for username (non-empty) and room (non-empty)
//      Re-prompt if either is blank.
//   4. Emit EVENTS.JOIN_ROOM with { username, room }
//   5. Export `username` and `room` as module-level variables
//      (Anuradha's files import these)
//   6. Implement leaveRoom():
//      - Emit EVENTS.LEAVE_ROOM with { room }
//      - (optional) clean up UI state
//   7. window.addEventListener('beforeunload', leaveRoom)
//
// RULE: Do NOT emit SEND_MESSAGE or handle receive_message here.
// ──────────────────────────────────────────────────────────

// Stub — implement in Phase 4
